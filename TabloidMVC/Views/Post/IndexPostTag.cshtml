@model TabloidMVC.Models.ViewModels.IndexPostTagViewModel

@{
    ViewData["Title"] = "IndexPostTag";
}

<h1>IndexPostTag</h1>

<p>
    <a asp-action="CreatePostTag" asp-route-id="@Model.Post.Id">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tag.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tag.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Tags) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
